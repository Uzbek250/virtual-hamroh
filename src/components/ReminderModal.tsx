import { FormEvent } from "react";
import { AnimatePresence, motion } from "motion/react";
import { X } from "lucide-react";

interface ReminderModalProps { open: boolean; text: string; time: string; onTextChange: (value: string) => void; onTimeChange: (value: string) => void; onSubmit: (e: FormEvent) => void; onClose: () => void; }
export default function ReminderModal({ open, text, time, onTextChange, onTimeChange, onSubmit, onClose }: ReminderModalProps) {
  return <AnimatePresence>{open && <div className="fixed inset-0 bg-slate-900/40 backdrop-blur-sm flex items-center justify-center p-4 z-50"><motion.div initial={{ scale: .95, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} exit={{ scale: .95, opacity: 0 }} className="bg-white rounded-3xl max-w-md w-full p-6 md:p-8 shadow-2xl border border-sky-100" id="add-reminder-modal"><div className="flex justify-between items-center mb-4 pb-2 border-b border-sky-100"><h3 className="text-base font-black text-indigo-900 uppercase tracking-wide">Yangi eslatma qo'shish</h3><button onClick={onClose} className="p-1.5 hover:bg-sky-100 rounded-lg transition-colors text-slate-400" aria-label="Modalni yopish"><X size={18} /></button></div><form onSubmit={onSubmit} className="flex flex-col gap-4"><div><label className="text-xs font-bold text-slate-500 block mb-1">Eslatma matni</label><input type="text" required value={text} onChange={(e) => onTextChange(e.target.value)} placeholder="Masalan: Ertangi darsga borish" className="w-full bg-slate-50 border-2 border-slate-100 rounded-2xl px-4 py-3 text-sm outline-none focus:border-indigo-400 focus:ring-2 focus:ring-indigo-100 text-slate-800 font-medium" id="manual-reminder-text" /></div><div><label className="text-xs font-bold text-slate-500 block mb-1">Trigger vaqti</label><input type="datetime-local" required value={time} onChange={(e) => onTimeChange(e.target.value)} className="w-full bg-slate-50 border-2 border-slate-100 rounded-2xl px-4 py-3 text-sm outline-none focus:border-indigo-400 focus:ring-2 focus:ring-indigo-100 text-slate-800 font-medium" id="manual-reminder-time" /></div><button type="submit" className="bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 rounded-2xl text-sm transition-all shadow-md">Eslatmani saqlash</button></form></motion.div></div>}</AnimatePresence>;
}
